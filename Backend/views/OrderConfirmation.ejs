<!DOCTYPE html>
<html>
  <head>
    <title>Order Confirmation</title>
  </head>
  <body>
    <h1>Order Confirmation</h1>
    <p>Hello, <%= userName %>!</p>
    <p>Your recent order details:</p>
    <table>
      <thead>
        <tr>
          <th>Image</th>
          <th>Product Name</th>
          <th>Quantity</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
        <% cartItems.forEach(function(item) { %>
        <tr>
          <td>
            <!-- Display image if available -->
            <% if (item.product_image) { %>
            <img
              src="<%= item.product_image %>"
              alt="<%= item.product_name %>"
              width="20"
              height="20"
            />
            <% } %>
          </td>
          <td><%= item.product_name %></td>
          <td><%= item.quantity %></td>
          <td>$<%= item.price %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
    <p>
      Important: This email is for testing purposes only, and no real
      transactions have occurred.
    </p>
  </body>
</html>
